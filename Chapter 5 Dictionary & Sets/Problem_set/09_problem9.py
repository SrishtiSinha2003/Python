#Can you change the vales inside a list which is contained in set S?

s={8,7,12,"Harry",[1,2]}

#first point - We can't include list in a set
#second point - if we could do it we can't change values by indexing.