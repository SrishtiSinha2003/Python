#replace the double space from problem 3 with single spaces.

name = "Aniket is a good  boy and  "

print(name.replace("  "," ")) #It does not changes your orginal string it just makes a new string.
print(name) # this is your same old string.

#Strings are immutable which means that you cannot change them by running functions on them.